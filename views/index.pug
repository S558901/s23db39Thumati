//- extends layout

//- block content
//-   h1= title
//-   p Welcome to #{title}
//-   p I am Sree Chetan Reddy Thumati
//-   button(type="button" class="btn btn-success" onclick="doReduce()") Press Me
//-   div#reduce Reduce the value
//-   script.
//-     let count = 20;
//-     let change = -1;

//-     function doReduce() {
//-       count += change;
//-       change -= 1;
//-       document.getElementById('reduce').textContent = `Count ${count} with change ${change}`;
//-     }

extends layout 
 
block content 
  if (!user) 
    a(href="/login") Login 
    br 
    a(href="/register") Register 
  if (user) 
    p You are currently logged in as #{user.username} 
    a(href="/telephone") Take me to telephone 
    br 
    a(href="/logout") Logout